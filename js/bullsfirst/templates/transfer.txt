<div class='modal-header'>Transfer</div>
<div class="transfer-tabbar" data-tabbar="transfer">
    <ul>
        <li><a href="#" data-tab="cash-tab" class="selected">Cash</a></li>
        <li><a href="#" data-tab="securities-tab">Securities</a></li>
    </ul>
</div>
<div id="transfer-forms-container">
    <form id="cash-transfer-form">
        <label for="fromAccountCash">From</label>
        <select id="fromAccountCash" name="fromAccount" class="validate[required]">
            {{#each accounts}}
                <option {{#ifEqual ../selectedAccount.id id}} selected="selected" {{/ifEqual}} value="{{id}}">{{name}} ( {{formatMoney marketValue}})</option>
            {{/each}}
        </select>
        <label for="toAccountCash">To</label>
        <select id="toAccountCash" name="toAccount" class="validate[required]">
            <option value=""> </option>
            {{#each accounts}}
                <option value="{{id}}">{{name}} ( {{formatMoney marketValue}})</option>
            {{/each}}
        </select>
        <label for="amount">Amount</label>
        <input
            id="amount"
            name="amount"
            type="text"
            class="validate[required,custom[number],min[0.01]] lastField">
      </form>
    <form id="securities-transfer-form" class="nodisplay">
        <label for="fromAccountSecurities">From</label>
        <select id="fromAccountSecurities" name="fromAccount" class="validate[required]">
            {{#each accounts}}
                <option {{#ifEqual ../selectedAccount.id idOption1}} selected="selected" {{/ifEqual}} value="{{id}}">{{name}} ( {{formatMoney marketValue}})</option>
            {{/each}}
        </select>
        <label for="toAccountSecurities">To</label>
        <select id="toAccountSecurities" name="toAccount" class="validate[required]">
            <option value=""> </option>
            {{#each accounts}}
                <option value="{{id}}">{{name}} ( {{formatMoney marketValue}})</option>
            {{/each}}
        </select>
        <div class="hboxContainer">
            <div class="symbol">
                <label for="symbol">Symbol</label>
                <input type="text" class="validate[required]" name="symbol" id="symbol">
            </div>
            <div class="quantity">
                <label for="quantity">Quantity</label>
                <input type="text" class="validate[required]" name="quantity" id="quantity">
            </div>
        </div>
        <label for="price">Price</label>
        <input
            id="price"
            name="price"
            type="text"
            class="validate[required]">
      </form>
    <a id="add-external-account-button" href="#"><span>Add an external account</span></a>
</div>
<a id="process-transfer-button" class="button large transfer-icon" href="#"><span>Transfer</span></a>

 